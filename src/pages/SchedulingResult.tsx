import React from "react";

/**
 * SchedulingResult
 *
 * Placeholder/empty page. You should create a route that renders this component at:
 *   /scheduling/result
 *
 * The Scheduling page already navigates to this path with navigation state:
 *   navigate("/scheduling/result", { state: { weekStart, jobs } })
 *
 * When you implement the schedule generation, read the navigation state (useLocation)
 * or fetch whatever server-side resource you prefer, then render the result here.
 *
 * NOTE: Page to be implemented
 */
const SchedulingResult: React.FC = () => {
   return <div className="p-6" />;
};

export default SchedulingResult;
